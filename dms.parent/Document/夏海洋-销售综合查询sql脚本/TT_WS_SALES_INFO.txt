CREATE
    TABLE TT_WS_SALES_INFO
    (
        DEALER_CODE VARCHAR (30) NOT NULL,
        SALES_ITEM_ID BIGINT (30)NOT NULL AUTO_INCREMENT,
        ITEM_ID BIGINT (30),
        SO_NO CHARACTER(12),
        VIN VARCHAR(17),
        CUSTOMER_NAME VARCHAR(120),
        CUSTOMER_NO CHARACTER(12),
        PRODUCT_CODE VARCHAR(300),
        REMARK VARCHAR(400),
        DATUM_ALL_SEND DECIMAL(8,0),
        SUBMIT_TIME DATETIME,
        AUDITING_STATUS DECIMAL(8,0),
        AUDITING_DATE DATETIME,
        AUDITING_REMARK VARCHAR(300),
        DATUM_SUBMIT_TIME DATETIME,
        DATUM_IS_UPLOAD DECIMAL(8,0),
        DAU_DOWN_TIMESTAMP DATETIME,
        DOWN_TIMESTAMP DATETIME,
        SERIALAPPLY_NO VARCHAR(30),
        DATUM_IS_SIGN DECIMAL(8,0) DEFAULT 12781002,
        OWNED_BY DECIMAL(14,0),
        D_KEY DECIMAL(1,0) DEFAULT 0,
        CREATED_BY DECIMAL(14,0),
        CREATED_AT DATETIME,
        UPDATED_BY DECIMAL(14,0),
        UPDATED_AT DATETIME,
        VER DECIMAL(5,0) DEFAULT 0,
        IS_SERIALAPPLY DECIMAL(8,0),
        SERIALAPPLY_DATE DATETIME,
        VEHICLE_REBATE DECIMAL(12,0),
        VEHICLE_SERIAL DECIMAL(8,0),
        EXAMINE_STATUS DECIMAL(8,0),
        CONSTRAINT P_WS_SALES_INFO PRIMARY KEY (SALES_ITEM_ID),
        CONSTRAINT F_WS_SALES_INFO FOREIGN KEY (ITEM_ID) REFERENCES TT_WS_CONFIG_INFO (ITEM_ID) ON
    DELETE
        RESTRICT ON
    UPDATE
        RESTRICT
    );ALTER TABLE TT_WS_CONFIG_INFO AUTO_INCREMENT=100000000000000;