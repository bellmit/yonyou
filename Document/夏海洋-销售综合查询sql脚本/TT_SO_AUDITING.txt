CREATE
    TABLE TT_SO_AUDITING
    (
        ITEM_ID BIGINT(30) NOT NULL AUTO_INCREMENT,
        DEALER_CODE VARCHAR(30) NOT NULL,
        SO_NO CHARACTER(12),
        AUDITING_RESULT DECIMAL(8,0),
        AUDITING_POSTIL VARCHAR(300),
        AUDITING_DATE DATETIME,
        AUDITED_BY DECIMAL(14,0),
        SUBMIT_AUDITED_BY DECIMAL(14,0),
        COMMIT_TIME DATETIME,
        TREATMENT_ADVICE VARCHAR(300),
        AUDITED_BY_IDENTITY DECIMAL(8,0),
        OWNED_BY DECIMAL(14,0),
        D_KEY DECIMAL(1,0) DEFAULT 0,
        CREATED_BY DECIMAL(14,0),
        CREATED_AT DATETIME,
        UPDATED_BY DECIMAL(14,0),
        UPDATED_AT DATETIME,
        VER DECIMAL(5,0) DEFAULT 0,
        DDCN_UPDATE_DATE DATETIME,
        CONSTRAINT P_SO_AUDITING PRIMARY KEY (ITEM_ID),
        CONSTRAINT F_SO_AUDITING FOREIGN KEY (DEALER_CODE, SO_NO) REFERENCES TT_SALES_ORDER (DEALER_CODE, SO_NO) ON
    DELETE
        RESTRICT ON
    UPDATE
        RESTRICT
    ); ALTER TABLE TT_SO_AUDITING AUTO_INCREMENT=100000000000000;