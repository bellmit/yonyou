CREATE
    TABLE TT_SO_PART
    (
        ITEM_ID BIGINT(30) NOT NULL AUTO_INCREMENT,
        DEALER_CODE VARCHAR(30) NOT NULL,
        SO_NO VARCHAR(12),
        PART_NO VARCHAR(27),
        PART_NAME VARCHAR(120),
        STORAGE_CODE CHARACTER(4),
        STORAGE_POSITION_CODE VARCHAR(30),
        UNIT_CODE CHARACTER(4),
        PART_QUANTITY DECIMAL(8,2),
        PART_SALES_PRICE DECIMAL(10,2),
        REAL_PRICE DECIMAL(12,2),
        ACCOUNT_MODE DECIMAL(8,0),
        PART_SALES_AMOUNT DECIMAL(12,2),
        PART_COST_PRICE DECIMAL(12,4),
        PART_COST_AMOUNT DECIMAL(12,4),
        REMARK VARCHAR(400),
        DISCOUNT DECIMAL(5,4),
        RO_NO VARCHAR(12),
        RO_PART_ITEM DECIMAL(14,0),
        IS_PRE_SALE DECIMAL(8,0),
        COMBO_CODE VARCHAR(15),
        OWNED_BY DECIMAL(14,0),
        D_KEY DECIMAL(1,0) DEFAULT 0,
        CREATED_BY DECIMAL(14,0),
        CREATED_AT DATETIME,
        UPDATED_BY DECIMAL(14,0),
        UPDATED_AT DATETIME,
        VER DECIMAL(5,0) DEFAULT 0,
        IS_FINISHED DECIMAL(8,0) DEFAULT 12781002,
        BATCH_NO DECIMAL(4,0),
        RECEIVER CHARACTER(4),
        SENDER CHARACTER(4),
        SEND_TIME DATETIME,
        NON_ONE_OFF DECIMAL(8,0),
        OUT_STOCK_NO CHARACTER(12),
        ADD_TAG DECIMAL(8,0) DEFAULT 12781002,
        PART_BATCH_NO VARCHAR(30),
        LOCK_USER CHARACTER(4),
        LEND_ID VARCHAR(200),
        LEND_QUANTITY DECIMAL(8,2) DEFAULT 0,
        RETURN_QUANTITY DECIMAL(8,2),
        WRITE_OFF_QUANTITY DECIMAL(8,2),
        THIS_LEND_QUANTITY DECIMAL(8,2),
        THIS_RETURN_QUANTITY DECIMAL(8,2),
        INTERIOR_PRICE DECIMAL(12,4),
        INTERIOR_AMOUNT DECIMAL(12,4),
        IS_FINISHEDR DECIMAL(8,0),
        DDCN_UPDATE_DATE DATETIME,
        CONSTRAINT P_SO_PART PRIMARY KEY (ITEM_ID),
        CONSTRAINT F_RS_SO_PART FOREIGN KEY (DEALER_CODE, SO_NO) REFERENCES TT_SALES_ORDER (DEALER_CODE, SO_NO) ON
    DELETE
        RESTRICT ON
    UPDATE
        RESTRICT
    );ALTER TABLE TT_SO_PART AUTO_INCREMENT=100000000000000;