CREATE
    TABLE TT_WS_CONFIG_INFO
    (
        ITEM_ID BIGINT(30) NOT NULL AUTO_INCREMENT,
        DEALER_CODE VARCHAR(30) NOT NULL,
        WS_NO VARCHAR(19),
        BRAND_CODE VARCHAR(300),
        SERIES_CODE VARCHAR(300),
        MODEL_CODE VARCHAR(300),
        CONFIG_CODE VARCHAR(300),
        PRODUCT_CODE VARCHAR(300),
        PURCHASE_COUNT DECIMAL(4,0),
        SUG_RETAIL_PRICE DECIMAL(10,2),
        WS_SUPPLY_END_DATE DATETIME,
        WS_INTENT_PRICE DECIMAL(10,2),
        WHOLESALE_DIRECTIVE_PRICE DECIMAL(12,2),
        WS_DISCOUNT_AMOUNT DECIMAL(12,2),
        WS_DISCOUNT_RATE DECIMAL(6,4),
        WS_SUPPLY_START_DATE DATETIME,
        SUPPLY_DAYS DECIMAL(8,0),
        CAT_CODE VARCHAR(30),
        OWNED_BY DECIMAL(14,0),
        D_KEY DECIMAL(1,0) DEFAULT 0,
        CREATED_BY DECIMAL(14,0),
        CREATED_AT DATETIME,
        UPDATED_BY DECIMAL(14,0),
        UPDATED_AT DATETIME,
        VER DECIMAL(5,0) DEFAULT 0,
        COLOR_CODE VARCHAR(8),
        IS_RES_APPLY DECIMAL(8,0) DEFAULT 12781002,
        CONSTRAINT P_WS_CONFIG_I PRIMARY KEY (ITEM_ID),
        CONSTRAINT F_WS_CONFIG_INFO FOREIGN KEY (DEALER_CODE, WS_NO) REFERENCES TT_PO_CUS_WHOLESALE (DEALER_CODE, WS_NO) ON
    DELETE
        RESTRICT ON
    UPDATE
        RESTRICT
    );ALTER TABLE TT_WS_CONFIG_INFO AUTO_INCREMENT=100000000000000;